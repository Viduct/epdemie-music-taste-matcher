# epdemie-music-taste-matcher
Like Tinder, but instead of swiping you have to listen to whole albums and rank them.

1. Install with `npm install`
2. Calculate with `npm run calculate`

Raw data is in `/data`
